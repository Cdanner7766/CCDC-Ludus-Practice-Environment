---
# Skeleton role for DC01 (Windows Server 2019 domain controller)
# TODO: extend with real AD / DNS config.

- name: Ensure WinRM basic connectivity works
  win_ping:

- name: Set hostname (optional, template should already do this)
  win_hostname:
    name: "{{ inventory_hostname }}"

# Placeholder for future domain creation / promotion tasks
- name: Placeholder - promote to domain controller (to be implemented)
  debug:
    msg: "Promote {{ inventory_hostname }} to DC for BLUE.LAB here."
